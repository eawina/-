#include <stdio.h>
void main()
{
	int a, b, c, i,count=0;
	for (i = 100; i <= 999; i++)
	{
		a = i % 10;
		b = i / 10 % 10;
		c = i / 100;
		if ((a + b + c) == 9)
		{
			printf("%d\n", i);
			count++;
		}
	}
	printf("共有%d个数\n", count);
		return 0;
}
