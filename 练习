#include <stdio.h>
int main(void)
{
    int i, sum, n;
    sum = 0;
    printf("请输入n的值\n");
    scanf("%d", &n);
    for (i = 1; i <= n; i++)
    {
        sum = sum + i;
    }
    i--;
    printf("1+2+...+ %d= %d\n", i, sum);
    return 0;
}
